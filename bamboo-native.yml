---
- hosts: all
  sudo: true
  vars:

    - user: bamboo
    - userfullname: "bamboo user"

    - psql_db: bamboo
    - psql_localhost_method: trust

  roles:
    - ubuntu-common
    - postgres
    - bamboo-server
